---
title: Win32 윈도우 생성 및 애플리케이션 루프 추가
type: Review
created: 2026-05-15T03:41
updated: 2026-05-15T04:25
completed: true
tags:
  - Review
  - Window
  - Application
---
# 일일 목표

- [x] Win32 API를 이용한 윈도우 생성
- [x] `main.cpp`에 애플리케이션 루프 추가

***
# 주요 내용

## 💻 코드

**window.cpp**

```C
bool Window::Initialize
(
	HINSTANCE hInstance,
	int nCmdShow,
	int width,
	int height,
	const wchar_t* title
)
{
    ...
	
    // 윈도우 생성
    m_hwnd = CreateWindowExW(
        0,                      // 확장 윈도우 스타일 (기본값)
        CLASS_NAME,             // 윈도우 클래스명
        m_title.c_str(),        // 윈도우 타이틀
        WS_OVERLAPPEDWINDOW,    // 윈도우 스타일
        CW_USEDEFAULT,          // 화면 X 좌표
        CW_USEDEFAULT,          // 화면 Y 좌표
        windowWidth,            // 윈도우 너비
        windowHeight,           // 윈도우 높이
        nullptr,                // 부모 윈도우
        nullptr,                // 메뉴 바 핸들
        m_hInstance,            // 인스턴스 핸들
        this                    // 추가 파라미터
    );
	
    ...
	
    return true;
}

```

`CreateWindowExW` 함수를 통해 **윈도우 객체를 생성**하였습니다. 
	`lpParam` 멤버를 `this` 포인터로 설정하여 이후 `StaticWindowProc` 함수가 해당 윈도우 객체의 주소를 식별할 수 있도록 작성하였습니다.

```C
LRESULT CALLBACK Window::StaticWindowProc
(
	HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam
)
{
    Window* window = nullptr;
	
    // 윈도우가 처음 만들어지는 시점인 경우
    if (uMsg == WM_NCCREATE)
    {
        // 윈도우 생성 정보 구조체 가져오기
        CREATESTRUCTW* createStruct = \ 
	        reinterpret_cast<CREATESTRUCTW*>(lParam);
        // CreateWindowEx의 lpParam으로 전달한 값 추출 (this 포인터)
        window = reinterpret_cast<Window*>(createStruct->lpCreateParams);
		
        // 추출한 객체 주소를 윈도우 내부 데이터 영역(GWLP_USERDATA)에 저장
        SetWindowLongPtrW
	        (
		        hwnd,
		        GWLP_USERDATA,
		        reinterpret_cast<LONG_PTR>(window)
	        );
			
        // CreateWindowExW으로 생성되었던 윈도우 핸들 보관
        window->m_hwnd = hwnd;
    }
	
    // 윈도우 생성 이후 시점인 경우
    else
    {
        // 윈도우 내부 데이터 영역(GWLP_USERDATA)에서 윈도우 객체 주소 추출
        window = \
	    reinterpret_cast<Window*>(GetWindowLongPtrW(hwnd, GWLP_USERDATA));
    }
	
    // 윈도우와 연결된 객체가 존재하지 않는 경우
    if (!window)
    {
        // 기본 윈도우 프로시저 호출
        return DefWindowProcW(hwnd, uMsg, wParam, lParam);
    }
	
    // WindowProc 함수 호출
    return window->WindowProc(uMsg, wParam, lParam);
}

LRESULT Window::WindowProc(UINT uMsg, WPARAM wParam, LPARAM lParam)
{
    switch (uMsg)
    {
    case WM_CLOSE:              // 사용자가 닫기 버튼을 눌렀을 때
        DestroyWindow(m_hwnd);  // 윈도우 제거 함수 호출
        return 0;
		
    case WM_DESTROY:            // 윈도우 제거 함수가 호출되었을 때
        PostQuitMessage(0);     // 애플리케이션 종료 함수 호출
        return 0;
		
    default:
        // 기본 윈도우 프로시저로 전달
        return DefWindowProcW(m_hwnd, uMsg, wParam, lParam);
    }
}
```

Windows 메시지 시스템과 윈도우 인스턴스를 연결하기 위해 윈도우 프로시저 구조를 `StaticWindowProc`과 `WindowProc`로 분리하였습니다.
OS로부터 전달받은 **윈도우 핸들**에 매핑된 `this` 포인터를 역참조함으로써, **특정 인스턴스의 멤버 함수가 메시지를 직접 처리할 수 있도록 구현하**였습니다.


**app.cpp**

```C
int App::Run()
{
    // 애플리케이션 실행 루프
    while (m_running)
    {
        // OS 메시지 확인 및 처리
        m_window.ProcessMessages(m_running);
		
        // 종료 메시지를 수신한 경우 루프 탈출
        if (!m_running)
        {
            break;
        }
		
        // 애플리케이션 내부 데이터 및 상태 업데이트
        Update();
        // 애플리케이션 화면 렌더링
        Render();
    }
	
    return 0;
}
```

애플리케이션 실행 루프를 구현하였습니다.


**main.cpp**

```C
#include "pch.h"
#include "app.h"

int WINAPI wWinMain(
    _In_ HINSTANCE hInstance,
    _In_opt_ HINSTANCE hPrevInstance,
    _In_ PWSTR lpCmdLine,
    _In_ int nCmdShow
)
{
    App app;
	
    if (!app.Initialize(hInstance, nCmdShow))
    {
        MessageBoxW
        (
	        nullptr,
	        L"Failed to initialize application.",
	        L"Error",
	        MB_OK | MB_ICONERROR
	    );
		
        return -1;
    }
	
    return app.Run();
}
```

`wWinMain` 함수에 애플리케이션 루프를 추가해 **App 인스턴스를 관리**하도록 작성하였습니다.


실행 결과 윈도우가 플로팅되었으며, ❎ 버튼을 통해 창이 제거되는 것을 확인하였습니다.

![[Screenshot-002.png|424]]


***
# Commit Link

https://github.com/MANGRYANG/GpuDrivenVFX/commit/68246e8e7b6ebe46e02e42e7b610ebe3bb632782

***

# 참조 문헌 및 링크

 - 윈도우 생성 관련 문서
	 https://learn.microsoft.com/ko-kr/windows/win32/learnwin32/creating-a-window

- 윈도우 프로시저 관련 문서
	https://learn.microsoft.com/ko-kr/windows/win32/learnwin32/writing-the-window-procedure

- Win32 API 관련 문서
	https://learn.microsoft.com/ko-kr/windows/win32/api/winuser/nf-winuser-createwindowexw
	https://learn.microsoft.com/ko-kr/windows/win32/api/winuser/ns-winuser-wndclassexw
	https://learn.microsoft.com/ko-kr/windows/win32/api/winuser/nf-winuser-setwindowlongptrw
	https://learn.microsoft.com/ko-kr/windows/win32/api/winuser/nf-winuser-peekmessagew
	https://learn.microsoft.com/ko-kr/windows/win32/api/winuser/nf-winuser-registerclassexw
	https://learn.microsoft.com/ko-kr/windows/win32/api/winuser/nf-winuser-adjustwindowrect