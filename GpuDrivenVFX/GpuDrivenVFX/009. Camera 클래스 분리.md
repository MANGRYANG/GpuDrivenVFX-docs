---
title: Camera 클래스 분리
type: Review
created: 2026-05-19T01:07
updated: 2026-05-19T01:52
completed: true
tags:
  - Review
  - Camera
  - Transform
---
# 일일 목표

- [x] 애플리케이션에서 관리하던 뷰/투영 변환 계산 과정을 Camera 클래스로 분리
- [x] 애플리케이션에서 변환 상수 버퍼를 생성할 때, 뷰/투영 변환 행렬을 요청하도록 구조 변경 

***
# 주요 내용

## 🏭 리팩토링

**camera.h**

 ```C
 #pragma once

#include <DirectXMath.h>

class Camera
{
public:
    // Camera 클래스 생성자
    Camera() = default;
    // Camera 클래스 소멸자
    ~Camera() = default;
	
    // 카메라가 바라보는 위치를 설정하는 함수
    void LookAt
    (
	    const DirectX::XMFLOAT3& position,
	    const DirectX::XMFLOAT3& target,
	    const DirectX::XMFLOAT3& up
	);
	
    // 원근 투영에 사용할 렌즈 값을 설정하는 함수
    void SetLens(float fovY, float aspectRatio, float nearZ, float farZ);
	
    // 뷰 변환 행렬 Getter
    DirectX::XMMATRIX GetViewMatrix() const;
    // 투영 변환 행렬 Getter
    DirectX::XMMATRIX GetProjectionMatrix() const;
	
private:
    // 카메라의 절대 위치
    DirectX::XMFLOAT3 m_position = DirectX::XMFLOAT3(0.0f, 0.0f, -2.0f);
    // 카메라의 초점
    DirectX::XMFLOAT3 m_target = DirectX::XMFLOAT3(0.0f, 0.0f, 0.0f);
    // 카메라의 상방 벡터
    DirectX::XMFLOAT3 m_up = DirectX::XMFLOAT3(0.0f, 1.0f, 0.0f);
	
    // 카메라 시야각 (45도로 초기화)
    float m_fovY = DirectX::XM_PIDIV4;
    // 화면 종횡비 (16:9으로 초기화)
    float m_aspectRatio = 16.0f / 9.0f;
    // 카메라 근평면 (0.1로 초기화)
    float m_nearZ = 0.1f;
    // 카메라 원평면 (100으로 초기화)
    float m_farZ = 100.0f;
};
 ```
 
 - 뷰 변환 및 투영 변환 행렬 계산 과정을 분리한 Camera 클래스를 생성하였습니다.

**app.cpp**

```C
void App::UpdateTransformBuffer(ID3D11DeviceContext* context)
{
    // 디바이스 컨텍스트나 변환 버퍼가 누락된 경우 실패 처리
    if (!context || !m_transformBuffer.Get())
    {
        return;
    }

    // 월드 변환 행렬
    const DirectX::XMMATRIX world = DirectX::XMMatrixIdentity();

    // 뷰 변환 행렬
    const DirectX::XMMATRIX view = m_camera.GetViewMatrix();

    // 투영 변환 행렬
    const DirectX::XMMATRIX projection = m_camera.GetProjectionMatrix();
```
 
 - 기존 `app.cpp`에서 수행하던 뷰/투영 변환 행렬 계산 과정을 제거하였습니다.
 - 뷰 변환 행렬과 투영 변환 행렬은 **카메라 객체에게 요청하여 제공받도록 수정**하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/GpuDrivenVFX/commit/dee03473d85f9b7df77ec14d09d32c498f0fd88c