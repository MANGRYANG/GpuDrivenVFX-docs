---
title: CPU Particle Emitter 기본 구조 추가
type: Review
created: 2026-05-26T03:20
updated: 2026-05-26T04:25
completed: true
tags:
  - Review
  - CPUParticleSystem
  - Emitter
---
# 일일 목표

- [x] Emitter 기본 구조 추가
- [x] Emitter 설정을 통해 Particle이 일정한 주기로 지속 생성되는 루틴을 구현

***
# 주요 내용

## 💻 코드

**particle/cpu_particle_system.h**

```C
// Particle을 일정 비율로 생성하기 위한 Emitter 상태 구조체
struct ParticleEmitter
{
    // Particle이 생성될 월드 공간 위치
    DirectX::XMFLOAT3 position = DirectX::XMFLOAT3(0, 0, 0);
    // 생성될 Particle의 초기 이동 속도
    DirectX::XMFLOAT3 velocity = DirectX::XMFLOAT3(0, 0, 0);
    // 생성될 Particle의 크기
    float particleSize = 0.0f;
    // 생성될 Particle의 생존 시간
    float particleLifetime = 0.0f;
    // 초당 생성할 Particle 개수
    float spawnRate = 0.0f;
    // 프레임마다 누적되는 생성 요청 수
    float spawnAccumulator = 0.0f;
    // 생성될 Particle의 색상
    DirectX::XMFLOAT4 particleColor = DirectX::XMFLOAT4(0, 0, 0, 0);
};
```

**Particle을 일정한 주기로 지속적으로 생성하기 위한 정책**을 기술하는 Emitter 구조체를 추가하였습니다. (`ParticleEmitter`)

```C
class CpuParticleSystem
{
	...
	
private:
    ...
	
    // Emitter의 누적 생성 요청을 처리하는 함수
    void EmitParticles(float deltaTime);
	
    ...
    
private:
    // 현재 테스트 단계에서 사용할 최대 Particle 개수
    static constexpr std::size_t MaxParticleCount = 64;
	
    // CPU Particle 생성 상태를 관리하는 Emitter
    ParticleEmitter m_emitter;
    
    ...
};
```

Emitter의 `spawnRate` 멤버를 기반으로 Particle 생성을 제어하는 `EmitParticles` 함수를 선언하여 **Particle의 런타임 생성 경로를 설정**하였습니다.

**최대 렌더링 가능한 Particle 개수를 64로 확장**하였으며, **Emitter 구조체 멤버 변수를 추가**하였습니다.


**particle/cpu_particle_system.cpp**

```C
void CpuParticleSystem::Initialize()
{
    // 기존 Particle 데이터 초기화
    m_particles.clear();
    // 기존 Billboard 데이터 초기화
    m_billboards.clear();
	
    // 테스트 단계에서 사용할 Particle 슬롯 공간 확보
    m_particles.resize(MaxParticleCount);
	
    // Emitter 상태 초기화
    m_emitter = ParticleEmitter
    {
        DirectX::XMFLOAT3(0.0f, -0.5f, 0.0f),       // 초기 월드 공간 위치
        DirectX::XMFLOAT3(0.0f, 0.25f, 0.0f),       // 초기 이동 속도
        0.01f,   // 크기
        4.0f,    // 생존 시간
        8.0f,    // 초당 생성해야 하는 Particle 개수
        0.0f,    // 프레임마다 누적되는 생성 요청 수
        DirectX::XMFLOAT4(1.0f, 0.65f, 0.1f, 1.0f)  // Particle 색상
    };
	
    // 초기 Particle 데이터를 렌더링용 Billboard 목록으로 변환
    RebuildBillboards();
}
```

`SpawnParticle` 함수를 직접 호출하여 Particle을 생성하던 기존 방식 대신, **Emitter 구조체 상태를 초기화**하고 이후 `Update` 단계에서 Emitter를 통해 Particle을 생성하도록 구조를 개선하였습니다.

```C

void CpuParticleSystem::Update(float deltaTime)
{
    
    // 활성 Particle의 수명 및 위치 갱신
    UpdateParticles(deltaTime);
	
    // Emitter의 spawnRate를 기준으로 새 Particle 생성 
    EmitParticles(deltaTime);
	
    // 현재 Particle 상태를 렌더링용 Billboard 목록으로 변환
    RebuildBillboards();
}
```

`Update` 함수에서 활성 상태의 Particle 상태를 갱신한 뒤, `EmitParticles` 함수를 호출하여 **Emitter를 통해 새로운 Particle을 생성**하도록 수정하였습니다.

```C
void CpuParticleSystem::EmitParticles(float deltaTime)
{
    // 생성해야 할 Particle 누적 개수 계산
    m_emitter.spawnAccumulator += m_emitter.spawnRate * deltaTime;
	
    // 누적된 생성 요청이 1개 이상이면 Particle 생성 시도
    while (m_emitter.spawnAccumulator >= 1.0f)
    {
        // Emitter 설정을 기반으로 새 Particle 생성 요청
        const bool spawned = SpawnParticle
        (
            m_emitter.position,
            m_emitter.velocity,
            m_emitter.particleSize,
            m_emitter.particleLifetime,
            m_emitter.particleColor
        );
		
        // 생성 요청 하나를 처리했으므로 누적 값 감소
        m_emitter.spawnAccumulator -= 1.0f;
		
        // 비활성 슬롯이 없어 생성하지 못한 경우 현재 프레임의 추가 생성 시도 중단
        if (!spawned)
        {
            break;
        }
    }
}
```

`EmitParticles` 함수의 내부 작동 방식은 다음과 같습니다.

- **`spawnRate`와 `deltaTime`을 기반으로 생성해야 할 Particle 누적 개수를 계산**
- 누적된 생성 요청이 존재하는 경우 `SpawnParticle` 함수를 통해 Particle 생성 시도
- 누적 생성 요청 개수(`spawnAccumulator`)를 1 감소
- **비활성 상태의 슬롯이 없어 생성에 실패한 경우 현재 프레임에서의 생성 중단**

## 🏭 리팩토링

 - 기존에 `Update` 단계에서 수행하던 **활성 Particle의 수명 관리 및 위치 갱신 작업**을 `UpdateParticles` 함수로 분리하였습니다.

---

실행 결과, **Particle이 (0.0, -0.4, 0.0)에서 생성**되어 **위쪽 방향으로 이동**하고, **3초가 지나면 비활성화**되는 것이 확인되었습니다.

![[Screenshot-021-01.png]]

***
# 이슈 및 버그

## 증상

 - 일정한 주기로 Particle이 생성되다가, **특정 간격으로 누락**되는 현상이 발생
 ![[Screenshot-021-02.png]]

## 재현

 - `MaxParticleCount` 값을 32로 설정

## 원인(추정)

 - 현재 `Initialize` 함수에서 Emitter의 `spawnRate` 값은 **8.0**, `particleLifetime` 값은 **4.0**으로 설정하고 있음
 - 이론적인 최대 활성 Particle 수는 `spawnRate`와 `particleLifetime`를 곱한 **32개**인데,  `MaxParticleCount`이 32로 설정되어 있어 **프레임 단위의 이산적 업데이트 과정에서 발생하는 일시적인 슬롯 경합**을 수용하지 못한 것으로 추정됨.

## 해결 및 우회 방법

 - `MaxParticleCount` 값을 **64로 확장**해 여유 슬롯을 확보하여 해결하였음

***
# Commit Link

https://github.com/MANGRYANG/GpuDrivenVFX/commit/f626c3b56f482c77944b2e0f2ac85b2279c13540