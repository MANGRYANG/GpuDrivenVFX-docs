---
title: 기본 셰이더 컴파일 구조 추가
type: Review
created: 2026-05-16T02:43
updated: 2026-05-16T03:32
completed: true
tags:
  - Review
  - Shader
---
# 일일 목표

- [x] 기본적인 정점 셰이더 파일 작성
- [x] 기본적인 픽셀 셰이더 파일 작성
- [x] 셰이더 컴파일 구조 추가
- [x] GPU 렌더링 파이프라인에 셰이더 바인딩

***
# 주요 내용

## 💻 코드

**VertexShader.hlsl**

```HLSL
// Vertex Buffer으로부터 입력받는 정점의 데이터 구조체
struct VSInput
{
    // 3차원 로컬 공간에서의 정점 위치 (X, Y, Z)
    float3 Pos : POSITION;
    // 정점에 대한 노멀 벡터 (X, Y, Z)
    float3 Normal : NORMAL;
    // 정점 고유의 색상 데이터 (R, G, B, A)
    float4 Color : COLOR;
};

// Vertex Shader 연산을 마친 후 래스터라이저로 넘길 데이터 구조체
struct VSOutput
{
    // 4차원 동차 좌표계 위치 (X, Y, Z, W)
    float4 Pos : SV_POSITION;
    // Vertex Shader 연산을 마친 노멀 벡터 (X, Y, Z)
    float3 Normal : NORMAL;
    // Vertex Shader 연산을 마친 색상 데이터 (R, G, B, A)
    float4 Color : COLOR;
};

// 각 정점마다 독립적으로 실행되는 정점 셰이더의 Entry Point
VSOutput VS_Main(VSInput input)
{
    VSOutput output;
    
    // GPU 파이프라인 규격에 맞게 동차 좌표계로 변환
    output.Pos = float4(input.Pos, 1.0f);
    // 정점 노말 벡터 데이터는 변환하지 않음
    output.Normal = input.Normal;
    // 정점 색상 데이터는 변환하지 않음
    output.Color = input.Color;
    
    return output;
}
```

기본적인 정점 셰이더를 구현하였습니다.


**PixelShader.hlsl**

```HLSL
// 래스터라이저에서 보간되어 전달된 정점의 데이터 구조체
struct VSOutput
{
    // 각 픽셀에 대한 4차원 동차 좌표계 위치 (X, Y, Z, W)
    float4 position : SV_POSITION;
    // 각 픽셀 위치에 맞게 보간된 노멀 벡터 (X, Y, Z)
    float3 Normal : NORMAL;
    // 각 픽셀 위치에 맞게 보간된 색상 데이터 (R, G, B, A)
    float4 color : COLOR;
};

// 각 픽셀마다 독립적으로 실행되는 픽셀 셰이더의 Entry Point
float4 PS_Main(VSOutput input) : SV_TARGET
{
    // 색상 데이터를 변환하지 않고 반환
    return input.color;
}
```

기본적인 픽셀 셰이더를 구현하였습니다.

**shader.cpp**

```C
bool Shader::CompileShaderFromFile(
    const std::wstring& filePath,
    const char* entryPoint,
    const char* target,
    Microsoft::WRL::ComPtr<ID3DBlob>& shaderBlob
)
{
    // 컴파일 시 사용할 특수 기능을 담는 변수
    UINT compileFlags = 0;
	
// Debug 모드로 빌드하는 경우 디버그 정보를 출력 코드에 삽입하고, 최적화하지 않게 설정
#if defined(_DEBUG)
    compileFlags |= D3DCOMPILE_DEBUG;
    compileFlags |= D3DCOMPILE_SKIP_OPTIMIZATION;
#endif
	
    // 에러 보관용 블롭
    Microsoft::WRL::ComPtr<ID3DBlob> errorBlob;
	
    // HLSL 셰이더 파일에 대한 컴파일 작업 수행
    HRESULT hr = D3DCompileFromFile
    (
        filePath.c_str(),                   // 파일 경로
        nullptr,                            // 매크로 정의 배열 (사용하지 않음)
        D3D_COMPILE_STANDARD_FILE_INCLUDE,  // Include 파일 처리기 설정
        entryPoint,                         // 셰이더의 Entry Point
        target,                             // 타겟 셰이더 모델
        compileFlags,                       // 셰이더 컴파일 옵션
        0,                                  // 효과 컴파일 옵션 (사용하지 않음)
        shaderBlob.GetAddressOf(),          // 셰이더 블롭 객체 포인터
        errorBlob.GetAddressOf()            // 에러 메시지 블롭 객체 포인터
    );
	
    // 셰이더 파일에 대한 컴파일에 실패한 경우
    if (FAILED(hr))
    {
        // 컴파일 에러 메시지가 존재하는 경우
        if (errorBlob)
        {
            // 디버깅 메시지 출력
            OutputDebugStringA
            (
	            static_cast<const char*>(errorBlob->GetBufferPointer())
	        );
        }
		
        // 메시지 박스 플로팅
        MessageBoxW
        (
	        nullptr,
	        L"Failed to compile shader. Check the Output window.",
	        L"Shader Compile Error",
	        MB_OK | MB_ICONERROR
	    );
		
        return false;
    }
	
    return true;
}
```

**HLSL 셰이더 파일을 읽어 와 컴파일**하고, 블롭 객체를 생성하는 함수입니다.

해당 함수를 통해 **정점 셰이더**와 **픽셀 셰이더** 파일을 컴파일하여 각각의 블롭 객체를 생성하고, 최종적으로 GPU에서 실행 가능한 셰이더 리소스로 변환하는 과정을 수행합니다.

```C
void Shader::Bind(ID3D11DeviceContext* context)
{
    // 정점 셰이더 객체를 정점 셰이더 단계에 바인딩
    context->VSSetShader(m_vertexShader.Get(), nullptr, 0);
	
    // 픽셀 셰이더 객체를 픽셀 셰이더 단계에 바인딩
    context->PSSetShader(m_pixelShader.Get(), nullptr, 0);
}
```

정점 셰이더 객체, 픽셀 셰이더 객체를 **각각의 GPU 렌더링 파이프라인에 맞게 바인딩**하는 함수를 구현하였습니다.


**app.cpp**

```C
bool App::Initialize(HINSTANCE hInstance, int nCmdShow)
{
    ...
    
    // 셰이더 컴파일에 실패한 경우 실패 처리
    if (!m_shader.Initialize(
		    m_renderer.GetDevice(),
		    L"shaders/VertexShader.hlsl",
		    L"shaders/PixelShader.hlsl"
		))
    {
        return false;
    }

    return true;
}
```

**애플리케이션 초기화 단계**에서 셰이더를 컴파일하는 로직을 추가하였습니다.

```C
void App::Render()
{
    // 프레임 드로우 준비 및 배경 색 초기화 (#0D141F)
    m_renderer.BeginFrame(0.05f, 0.08f, 0.12f, 1.0f);
	
    // 셰이더 바인딩
    m_shader.Bind(m_renderer.GetContext());
	
    // 최종 화면 출력
    m_renderer.EndFrame();
}
```

**애플리케이션 Render 단계**에서 셰이더를 GPU 렌더링 파이프라인에 바인딩하였습니다.


## ⚙️ 프로젝트 설정

### HLSL 진입점 설정

- `VertexShader.hlsl` 파일의 **Entry Point를 설정**하였습니다. (`VS_Main`)
- `PixelShader.hlsl` 파일의 **Entry Point를 설정**하였습니다. (`PS_Main`)

***
# Commit Link

https://github.com/MANGRYANG/GpuDrivenVFX/commit/879253737cd38acc9cb60155a6b4776417756ce7

***

# 참조 문헌 및 링크

 - 셰이더 객체 생성 함수 관련 문서
	 https://learn.microsoft.com/ko-kr/windows/win32/api/d3d11/nf-d3d11-id3d11device-createvertexshader
	 https://learn.microsoft.com/ko-kr/windows/win32/api/d3d11/nf-d3d11-id3d11device-createpixelshader

- HLSL 파일 컴파일 함수 관련 문서
	https://learn.microsoft.com/ko-kr/windows/win32/api/d3dcompiler/nf-d3dcompiler-d3dcompilefromfile

- 셰이더 모델 관련 문서
	https://learn.microsoft.com/ko-kr/windows/win32/direct3dhlsl/dx-graphics-hlsl-models