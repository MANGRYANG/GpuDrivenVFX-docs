---
title: Particle Emitter 설정 통합 관리
type: Review
created: 2026-06-08T17:17
updated: 2026-06-08T19:35
completed: true
tags:
  - Review
  - Refactor
---
# 일일 목표

- [x] CPU/GPU 파티클 시스템에서의 Emitter 설정 통일
- [x] 공통 Emitter 설정을 Config 파일로 통합 관리

***
# 주요 내용

## 🏭 리팩토링

**particle/particle_config.h**

```C
#pragma once

#include <DirectXMath.h>
#include <cstddef>

namespace ParticleConfig
{
    // CPU, GPU Particle System에서 사용할 최대 Particle 슬롯 개수
    constexpr std::size_t ParticleCapacity = 64;
	
    // 공통 Emitter 위치
    inline const DirectX::XMFLOAT3 EmitterPosition =
	    DirectX::XMFLOAT3(0.0f, -0.5f, 0.0f);
	
    // 공통 Emitter 초기 속도
    inline const DirectX::XMFLOAT3 EmitterVelocity =
	    DirectX::XMFLOAT3(0.0f, 0.25f, 0.0f);
	
    // 공통 Particle 크기
    constexpr float ParticleSize = 0.01f;
	
    // 공통 Particle 생존 시간
    constexpr float ParticleLifetime = 3.0f;
	
    // 공통 초당 생성 개수
    constexpr float SpawnRate = 10.0f;
}
```

**각 파티클 시스템에서 사용하는 공통 Emitter 설정**을 **파티클 Config 파일에서 통합 관리**하도록 수정하였습니다.

**particle/cpu_particle_system.cpp**

```C
bool CpuParticleSystem::SpawnParticle
(
    const DirectX::XMFLOAT3& position,
    const DirectX::XMFLOAT3& velocity,
    float size,
    float lifetime,
    const DirectX::XMFLOAT4& color
)
{
    // 사용할 수 있는 Particle 슬롯이 없는 경우
    if (m_particles.empty())
    {
        ++m_droppedSpawnCount;
        return false;
    }
	
    // 이번 프레임에 생성할 Particle 슬롯 선택
    const std::size_t particleIndex = m_emitter.spawnIndex;
    Particle& particle = m_particles[particleIndex];
	
    // 다음 생성 슬롯을 순환 방식으로 갱신
    m_emitter.spawnIndex = (m_emitter.spawnIndex + 1) % m_particles.size();
	
    // 슬롯 인덱스를 사용해 수평 퍼짐을 부여
    const float spread = static_cast<float>(particleIndex % 4) - 2.0f;
	
    // Particle 위치 설정
    particle.position = DirectX::XMFLOAT3
    (
        position.x + spread * 0.03f,
        position.y,
        position.z
    );
	
    // Particle 속도 설정
    particle.velocity = DirectX::XMFLOAT3
    (
        velocity.x + spread * 0.03f,
        velocity.y,
        velocity.z
    );
	
    // Particle 렌더링 크기 설정
    particle.size = size;
    // Particle 생존 시간 설정
    particle.lifetime = lifetime;
    // Particle 경과 시간 초기화
    particle.age = 0.0f;
    // Particle 색상 설정
    particle.color = color;
    // Particle 활성화
    particle.active = true;
	
    return true;
}
```

CPU 파티클 시스템에서도 **GPU 파티클 시스템과 같이 spread를 사용해 수평 퍼짐을 부여하도록 수정**하였습니다.

이를 위해 **CPU 파티클 시스템에서 Ring Buffer 구조를 사용하여 파티클 슬롯을 관리**하도록 설정하였습니다.

___

실행 결과, CPU 파티클 시스템과 GPU 파티클 시스템에서 보여지는 파티클의 생성 패턴이 동일하게 변경되었습니다.

![[Screenshot-033-01.png]]

![[Screenshot-033-02.png]]

***
# Commit Link

https://chatgpt.com/g/g-p-6a0545000a20819188be1c3e80c46007/c/6a10affa-0bcc-83a2-8c7b-6dc89871e177

https://github.com/MANGRYANG/GpuDrivenVFX/commit/50c7716fe781d5b86f77dc591df6c6e055fe88ac